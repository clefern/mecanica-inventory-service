name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  test:
    uses: Overz/fiap-tc-mecanica-pipelines/.github/workflows/test.yml@main
    with:
      service-name: inventory-service
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
